fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.3
pydantic-settings==2.9.1
python-dotenv==1.1.0
httpx==0.28.1

# Database
sqlalchemy[asyncio]==2.0.51
asyncpg==0.30.0
alembic==1.18.5

# Trend Analyzer
apscheduler==3.11.3
# pytrends (last released Apr 2023, unmaintained, breaks on Google anti-bot
# changes) replaced by pytrends-modern: same sync API, built-in retry/backoff
# and user-agent rotation, actively maintained.
pytrends-modern==0.2.12
# Reddit's public .json endpoints return 403 Blocked in practice (confirmed
# live) — praw uses OAuth read-only mode instead, Reddit's supported path.
praw==8.0.2
feedparser==6.0.12
langgraph==1.2.9
anthropic==0.116.0

# Knowledge Base + Company Analyzer
voyageai==0.4.1
pgvector==0.4.2
trafilatura==2.1.0
# trafilatura → justext still imports lxml.html.clean, which was split out
# of lxml core in 5.x. Pin the standalone package so the import chain works.
lxml_html_clean==0.4.3
langchain-text-splitters==1.1.2
# Business document uploader — text extraction for PDF/DOCX, plus
# python-multipart for FastAPI's UploadFile/Form parsing
pypdf==6.14.2
python-docx==1.2.0
python-multipart==0.0.32

# Social Media Analyzer — Composio brokers OAuth + custodies tokens; using
# the low-level REST resource client (not the `composio` package) avoids
# pulling in its `openai`/`pysher` deps and a forced pydantic bump this
# app's other pinned dependencies don't need.
composio-client==1.42.0
